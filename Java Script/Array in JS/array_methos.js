// slice method used to give contegeous segment of the array 

let arr = [1,2,3,4,5,6];
consolelog(arr.slice(2,5)); // this slice method copied the value of the array means it return the copied array value with new memory location

// we can use it as well arr[0:3];